<!DOCTYPE html>
html

  <head>
    <meta charset="utf-8">
    <title>My Blog</title>
    <link rel="stylesheet" href = "/css/index.css">
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  </head>

  <body>

    <header>
        if isAuthenticated==true //로그인이 되어있으면 로그아웃 출력
          a( href="/logout" id="logout") 로그아웃
        else  // 로그인이 되어있지 않으면 로그인 출력
          a( href="/login" id="login") 로그인
        div(id="headTitle") My blog
        <ul id="nav">
          <li><a href="/">일기장</a></li>
          <li><a href="/gallery">사진첩</a></li>
        </ul>       
    </header>


    if menu=='diary'
      if isAuthenticated == true // 로그인 되어있으면 +버튼과 수정,삭제버튼이생김(admitTrue.js)
        a(href="/user/textForm?type=write")
          div(id="addbtn")
            p + 
        include diary
        <script src='admitTrue.js'></script>
      else // 로그인이 되어있지 않으면 +, 수정, 삭제버튼이 없음(admitFalse.js)
        include diary 
        script(src='admitFalse.js')
    else if menu =='gallery'
      include gallery
    
  </body>
